#define ll long long
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false); cin.tie(0);
    int t;
    cin>>t;
    while(t--){
        ll n,m,x,y;
        cin>>n>>m>>x>>y;
        ll temp1=n,temp2=m;
        while (n--)
        {
           ll num=0;
           cin>>num;
        }
        while (m--)
        {
           ll num=0;
           cin>>num;
        }
        cout<<temp1+temp2<<endl;
    }
    return 0;
}